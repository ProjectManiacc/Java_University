/**
 * The controller package manages the application's logic and user input.
 * It includes the CipherController class, which handles reading user sentences,
 * encrypting sentences, and defines a custom exception for invalid characters.
 * 
 * @author Piotr Kluziok
 */
package controller;
